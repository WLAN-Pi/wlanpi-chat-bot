---
 command: set_hostname
 exec: "/opt/wlanpi-common/wlanpi-hostname.sh set "
 progress_msg: Setting hostname...
 emoji: page
 help_short: Set WLAN Pi hostname (set hostname my_hostname)
 help_long: Set WLAN Pi hostname (set hostname my_hostname)